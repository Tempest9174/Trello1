# タスク管理Webアプリケーション 要件定義書

---

## ドキュメント情報

| 項目 | 内容 |
|------|------|
| 文書番号 | REQ-001 |
| バージョン | 1.0 |
| 作成日 | 2026-04-27 |
| 作成者 | masa |

### 改訂履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| 1.0 | 2026-04-27 | 初版作成 |

---

## 1. このアプリについて

本アプリは、タスクをカード形式で管理できるWebアプリケーションです。
「やること」「進行中」「完了」のような列を作り、カードを並べたり移動させたりしながら作業を管理できます。
複数のボード（作業スペース）を切り替えて使うことができます。

---

## 2. できること・できないこと

### 2.1 必須機能

| 要件ID | 機能名 | 内容 |
|--------|--------|------|
| FR-01 | ボード管理 | 複数のボード（例：仕事用・個人用）を作成・切り替え・削除できる |
| FR-02 | リスト管理 | ボード内に列（リスト）を追加・名前変更・削除・並び替えができる |
| FR-03 | カード管理 | カードにタイトル・説明・期限日・優先度・ラベルを設定できる |
| FR-04 | ドラッグ操作 | カードをマウスで掴んで別の列や別の位置に移動できる |
| FR-05 | ラベル管理 | 色と名前をつけたラベルを作成し、カードに貼り付け・取り外しができる |

**FR-01 ボード管理**

ボードはアプリの「作業スペース」です。「仕事用」「個人用」のように用途ごとに複数作ることができ、ホーム画面からいつでも切り替えられます。ボードのタイトルは後から変更でき、不要になったボードは削除できます（削除するとボード内のすべてのデータも消えます）。

**FR-02 リスト管理**

リストはボード内の「列」です。「やること」「進行中」「完了」のように、作業の状態に合わせて自由に作れます。リストの名前はクリックして変更でき、不要なリストは削除できます。列の順番はドラッグ操作で入れ替えることができます。

**FR-03 カード管理**

カードは1件のタスクを表します。カードには以下の情報を設定できます。

- **タイトル**（必須）：タスクの名前
- **説明**（任意）：タスクの詳細メモ
- **期限日**（任意）：カレンダーから日付を選択
- **優先度**（高・中・低）：デフォルトは「中」
- **ラベル**（任意）：色付きタグを複数付与できる

**FR-04 ドラッグ操作**

カードをマウスで掴んで別の列へ移動したり、同じ列内で上下に並び替えることができます。列そのものも左右にドラッグして並び順を変えられます。移動後の状態はページを更新しても保持されます。

**FR-05 ラベル管理**

ラベルは色と名前を自由に設定できる「色タグ」です。ボードごとに作成・管理し、カードに貼り付けたり取り外したりできます。1枚のカードに複数のラベルをつけることができます。

---

### 2.2 フェーズ2以降で対応予定のオプション機能

| 要件ID | 機能名 | 内容 |
|--------|--------|------|
| OP-01 | 複数ボード切り替え | 用途ごとに複数のボードを作成し、ホーム画面から自由に切り替えられる |

---

### 2.3 このアプリではできないこと（対象外）

- ログイン・パスワード管理（1人で使う前提のため、認証機能はありません）
- ファイルの添付
- 複数人での同時編集・リアルタイム同期
- スマートフォン・タブレットへの最適化

---

### 2.4 利用環境の制約

| 項目 | 内容 |
|------|------|
| 対応ブラウザ | Google Chrome（最新版）・Microsoft Edge（最新版） |
| 推奨画面サイズ | 横幅1280px以上のパソコン画面 |
| 利用環境 | ローカル環境での動作（社内サーバーまたは開発用PC） |
| データの保持 | サーバーを停止してもデータは消えません |

---

## 3. 各機能の詳細説明

### カードの「優先度」について

カードには「高・中・低」の3段階で優先度を設定できます。優先度はカードの一覧にバッジとして表示されるため、重要なタスクをひと目で確認できます。デフォルト（初期値）は「中」です。

### カードの「期限日」について

カードに期限日を設定すると、カードに日付が表示されます。期限が近いタスクを視覚的に把握するのに役立ちます。期限日の設定は任意です。

### 削除操作について

ボード・リスト・カードは削除できます。削除した場合、その中にあるデータも一緒に消えます（例：ボードを削除すると、そのボード内のリストとカードもすべて消えます）。削除は取り消せませんのでご注意ください。

---

## 4. 画面の説明

### 4.1 画面遷移図

```
┌─────────────────┐
│   ホーム画面     │  ← アプリ起動時に表示
│  （ボード一覧）  │
└────────┬────────┘
         │ ボードをクリック
         ▼
┌─────────────────┐       ┌──────────────────────┐
│   ボード画面     │──────▶│  カード詳細画面       │
│  （メイン画面）  │       │  （カードをクリック） │
└─────────────────┘       └──────────────────────┘
```

---

### 4.2 ホーム画面（ボード一覧）

アプリを開いたときに最初に表示される画面です。作成したボードの一覧が並んでいます。

```
┌────────────────────────────────────────────────────────┐
│  タスク管理アプリ                                       │  ← ヘッダー
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │
│  │  仕事用  │  │  個人用  │  │  学習用  │  ← ボードカード
│  │  [編集]  │  │  [編集]  │  │  [編集]  │
│  │  [削除]  │  │  [削除]  │  │  [削除]  │
│  └──────────┘  └──────────┘  └──────────┘
│                                                        │
│  ┌────────────────┐                                   │
│  │  + 新しいボード │                                   │
│  └────────────────┘                                   │
└────────────────────────────────────────────────────────┘
```

**この画面でできること：**
- ボードをクリックするとそのボードの画面に移動する
- [編集] でボードのタイトルをクリックして書き換えられる
- [削除] でボードとその中身をすべて削除する（確認ダイアログあり）
- [+ 新しいボード] でタイトルを入力して新しいボードを作成する

---

### 4.3 ボード画面（メイン画面）

ボードをクリックすると表示される画面です。列（リスト）とカードを管理する中心的な画面です。

```
┌────────────────────────────────────────────────────────────────────┐
│  ← 戻る  |  仕事用ボード  [タイトル編集]                           │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │
│  │ やること [編集削除]  進行中 [編集削除]  完了   [編集削除]        │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤
│  │ [高]         │  │ [中]         │  │ [低]         │  ← 優先度バッジ
│  │ タスクA      │  │ タスクC      │  │ タスクE      │
│  │ 期限: 5/1   │  │ ラベル:バグ  │  │              │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤
│  │ + カード追加 │  │ + カード追加 │  │ + カード追加 │
│  └──────────────┘  └──────────────┘  └──────────────┘
│                                                                    │
│  ┌───────────┐                                                     │
│  │ + リスト追加 │                                                   │
│  └───────────┘                                                     │
└────────────────────────────────────────────────────────────────────┘
```

**この画面でできること：**
- カードをドラッグして別の列や同じ列の別の位置に移動できる
- 列（リスト）をドラッグして左右に並び替えられる
- カードをクリックするとカード詳細画面が開く

**カードに表示される情報：**

| 表示要素 | 表示されるタイミング |
|---------|-------------------|
| カードタイトル | 常に表示 |
| 優先度バッジ（高・中・低） | 常に表示 |
| 期限日（例：5/1） | 期限日が設定されている場合 |
| ラベル（色帯） | 1つ以上ラベルが設定されている場合 |

---

### 4.4 カード詳細画面

カードをクリックすると表示されるポップアップ画面です。タスクの詳細を確認・編集できます。

```
┌───────────────────────────────────────────────────────────┐
│  [× 閉じる]                                               │
│                                                           │
│  タイトル                                                  │
│  ┌────────────────────────────────────────────────────┐  │
│  │ タスクAのタイトル                                   │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  優先度: [高 ▼]  （高・中・低から選択）                    │
│                                                           │
│  期限日: [ 2026-05-01 ]                                   │
│                                                           │
│  ラベル                                                    │
│  ┌─────────────────────────────────────┐                 │
│  │ [バグ ×]  [機能追加 ×]              │                 │
│  │ [+ ラベルを追加]                    │                 │
│  └─────────────────────────────────────┘                 │
│                                                           │
│  説明                                                     │
│  ┌────────────────────────────────────────────────────┐  │
│  │  （ここにメモを書く）                               │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  [保存]                            [カードを削除]         │
└───────────────────────────────────────────────────────────┘
```

**この画面でできること：**
- タイトル・説明・期限日・優先度・ラベルを編集して [保存] で確定する
- [カードを削除] でカードを削除する
- [× 閉じる] または画面外をクリックして閉じる

---

### 4.5 ラベル選択画面

カード詳細画面の [+ ラベルを追加] をクリックすると表示されます。

```
┌──────────────────────────┐
│  ラベルを選択             │
│ ─────────────────────── │
│  [✓] バグ                │
│  [ ] 機能追加            │
│  [ ] 緊急                │
│ ─────────────────────── │
│  + 新しいラベルを作成    │
└──────────────────────────┘
```

**この画面でできること：**
- チェックを入れるとカードにラベルが付く、外すと取り外せる
- [+ 新しいラベルを作成] で色と名前を設定した新しいラベルを追加できる

---

## 5. 受け入れ基準（完成の定義）

各機能が「完成した」とみなせる操作上の条件です。

### FR-01 ボード管理

- [+ 新しいボード] ボタンを押してタイトルを入力すると、一覧に新しいボードが表示される
- ボードのタイトルをクリックして書き換えると、画面に反映される
- ボードを削除すると、そのボード内のすべてのデータも一緒に消える
- ページを再読み込みしてもボードのデータが残っている

### FR-02 リスト管理

- リストを追加するとボード画面に新しい列が表示される
- リストのタイトルをクリックして書き換えられる
- リストを削除するとその列内のカードもすべて削除される
- リストを左右にドラッグして並び替えができる
- ページを再読み込みしてもリストのデータが残っている

### FR-03 カード管理

- [+ カード追加] からカードを作成すると列に表示される
- カードをクリックするとポップアップが開き、タイトル・説明・期限日・優先度・ラベルを編集できる
- [保存] ボタンを押すと変更が一覧にも反映される
- ページを再読み込みしてもカードの内容が残っている

### FR-04 ドラッグ操作

- カードを掴んで別の列にドロップすると、その列に移動する
- カードを同じ列内の別の位置にドラッグして順番を変えられる
- ページを再読み込みしても移動後の位置が保持されている
- 列を掴んで左右にドラッグして並び順を変えられる

### FR-05 ラベル管理

- ラベルを新規作成するとき、名前と色を設定できる
- カードのポップアップからラベルをカードに付与・取り外しできる
- 1枚のカードに複数のラベルをつけられる
- ラベルを削除するとそのラベルを付けていたカードからも外れる

---

## 6. システム構成（概要）

本アプリは以下の3層で構成されています。

```
┌─────────────────────────────────────┐
│  ブラウザ（お客さまが使う画面）       │
│  タスクの表示・操作を担当する         │
└────────────────┬────────────────────┘
                 │ データのやり取り
┌────────────────▼────────────────────┐
│  サーバー（データ処理を行う部分）     │
│  保存・更新・削除の処理を担当する     │
└────────────────┬────────────────────┘
                 │ 保存・読み出し
┌────────────────▼────────────────────┐
│  データベース（データを保存する場所）  │
│  ボード・リスト・カード等を永続保存    │
└─────────────────────────────────────┘
```

### 保存されるデータの種類

ボードの中に複数のリストがあり、各リストの中に複数のカードがある構造です。ラベルはボード単位で作成し、カードに自由に付与できます。ボードを削除すると配下のリスト・カードもすべて自動で削除されます。

---

## 7. 開発スケジュール

### フェーズ1：データと処理の構築

データの保存・取得・更新・削除の基本機能を構築します。

| ステップ | 内容 |
|---------|------|
| Step 1 | データベースの設計・構築 |
| Step 2 | ボード機能の実装・動作確認 |
| Step 3 | リスト機能の実装・動作確認 |
| Step 4 | カード機能の実装・動作確認 |
| Step 5 | ラベル機能の実装・動作確認 |
| Step 6 | エラー処理の整備 |

### フェーズ2：画面の構築

ユーザーが操作する画面を構築し、フェーズ1の機能と接続します。

| ステップ | 内容 |
|---------|------|
| Step 7 | 開発環境のセットアップ |
| Step 8 | ボード一覧画面の実装 |
| Step 9 | ボード詳細画面の実装 |
| Step 10 | カード詳細画面の実装 |
| Step 11 | ドラッグ操作の実装 |
| Step 12 | 総合動作確認 |
